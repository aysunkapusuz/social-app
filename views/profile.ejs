<%- include('partials/header') -%>
<div class="container">
      <div class="row justify-content-center mt-5" >
        <section class="col-6 mt-5">
              <p><strong>User Name</strong>: <%= user.userName %></p>
              <p><strong>Email</strong>: <%= user.email %></p>
              <div class="row justify-content-between mt-5">
                <a class="col-5 btn btn-outline-warning" href="/feed">Return to Feed</a>
                <a href="/logout" class="col-5 btn btn-outline-warning">Logout</a>
              </div>
              </section>
      </div>
          <div class="row justify-content-center form-floating ">
            <section class="col-6 mt-5">
            <h2>Add a post</h2>
            <form action="/post/createPost" enctype="multipart/form-data" method="POST">
              <div class="mb-3">
                  <label for="title" class="form-label">Title</label>
                  <input type="text" class="form-control" id="title" name="title">
              </div>
              <div class="mb-3">
                <label for="caption" class="form-label">Caption</label>
                <textarea class="form-control" id="caption" name="caption"></textarea>
              </div>
              <div class="mb-3">
                <label for="imgUpload" class="form-label">Image</label>
                <input type="file" class="form-control form-control-sm" id="imageUpload" name="file">
              </div>
              <button type="submit" class="btn btn-secondary" value="Upload">Submit</button>
            </form>
          </section>
          </div>
      <div class="row mt-5">
        <ul class="row list-unstyled">
        <% for(var i=0; i<posts.length; i++) {%>
          <li class="col-6 justify-content-between mt-5">
            <a href="/post/<%= posts[i]._id%>">
              <img class="img-thumbnail" src="<%= posts[i].image%>">
            </a>
          </li>
        <% } %>
        </ul> 
      </div>  
    </div>
  </div>

<%- include('partials/footer') -%>